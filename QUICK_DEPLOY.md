# Быстрое развертывание на хостинге

## Перед развертыванием

### 1. Обновите настройки API

Откройте `web_app.py` и найдите:

```python
API_CONFIG = {
    'base_url': 'http://your-bot-server.com:5001',  # ⚠️ Замените на реальный URL
}
```

Укажите реальный IP адрес или домен вашего сервера с ботом.

### 2. Убедитесь, что API сервер бота запущен

На сервере бота должен работать `bot_api_server.py`:
```bash
python bot_api_server.py
```

Проверьте доступность:
```bash
curl http://your-bot-server-ip:5001/api/health
```

## Варианты развертывания

### 🚀 PythonAnywhere (самый простой)

1. Зарегистрируйтесь на [pythonanywhere.com](https://www.pythonanywhere.com)
2. Загрузите файлы через веб-интерфейс
3. Создайте Flask веб-приложение
4. Укажите путь к `web_app.py`
5. Обновите WSGI файл (см. `DEPLOYMENT.md`)
6. Перезагрузите приложение

### 🚀 Heroku

```bash
# Установите Heroku CLI
heroku login
heroku create your-app-name
git push heroku main
heroku config:set API_BASE_URL=http://your-bot-server-ip:5001
```

### 🚀 VPS (Ubuntu/Debian)

```bash
# 1. Установите зависимости
sudo apt update
sudo apt install python3-pip python3-venv nginx

# 2. Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pip install gunicorn

# 3. Запустите через Gunicorn
gunicorn --bind 0.0.0.0:5000 web_app:app

# 4. Настройте Nginx (см. DEPLOYMENT.md)
```

### 🚀 Shared хостинг (cPanel)

1. Загрузите файлы через FTP
2. В cPanel найдите "Setup Python App"
3. Создайте приложение
4. Укажите путь к `web_app.py`
5. Установите зависимости

## Важные настройки

### Для продакшена:

1. **Отключите debug:**
   ```python
   # В web_app.py уже настроено автоматически
   # Установите переменную окружения:
   export FLASK_ENV=production
   ```

2. **Установите SECRET_KEY:**
   ```python
   # В web_app.py или через переменную окружения
   export SECRET_KEY=your-secret-key-here
   ```

3. **Настройте HTTPS:**
   - Используйте Let's Encrypt
   - Или настройте через панель хостинга

## Проверка после развертывания

1. ✅ Сайт открывается
2. ✅ Авторизация работает
3. ✅ Данные загружаются
4. ✅ Статические файлы (CSS/JS) работают
5. ✅ Экспорт в Excel работает

## Решение проблем

**Ошибка подключения к API:**
- Проверьте `API_CONFIG['base_url']`
- Убедитесь, что API сервер бота запущен
- Проверьте firewall

**Статические файлы не загружаются:**
- Проверьте путь к папке `static`
- Проверьте права доступа

**Ошибка 500:**
- Проверьте логи приложения
- Убедитесь, что все зависимости установлены

## Подробная инструкция

См. `DEPLOYMENT.md` для детальной инструкции по каждому типу хостинга.

